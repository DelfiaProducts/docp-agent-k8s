{{- if .Values.manager.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.docp.name }}
  namespace: {{ .Values.namespace }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.docp.data }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}
