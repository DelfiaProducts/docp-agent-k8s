{{- if .Values.manager.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.orya.name }}
  namespace: {{ .Values.namespace }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.orya.data }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}
