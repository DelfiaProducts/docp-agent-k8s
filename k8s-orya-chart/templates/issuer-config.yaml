# k8s-orya-chart/templates/issuer-config.yaml
{{- if .Values.issuer.enabled }}
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ .Values.issuer.name }} 
  namespace: {{ .Values.namespace }}
spec:
  selfSigned: {}
{{- end }}
